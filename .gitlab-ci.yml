stages:
- test

test-runner:
  stage: test
  image: docker:latest # Use a Docker-enabled image
  services:
  - docker:dind # Enable Docker-in-Docker
  variables:
    DOCKER_DRIVER: overlay2
  script:
  - echo "✅ GitLab Runner is working!"
  - uname -a
  - docker --version # This should now work
  - docker ps # List running containers
  - docker run hello-world # Run a test container
